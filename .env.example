# Optional: enable OpenAI-powered responses for the UniFlora AI Assistant.
# Without this key, the assistant uses the built-in knowledge base (plant catalogue data).
OPENAI_API_KEY=

# Model for plant Q&A (default: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
